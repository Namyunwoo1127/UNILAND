<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.elon.boot.domain.community.notice.model.store.NoticeMapper">

   <!-- 전체 공지사항 개수 조회 (삭제되지 않은 것만) -->
    <select id="getTotalCount" resultType="_int">
        SELECT COUNT(*) 
        FROM NOTICE_TBL
        WHERE DELETE_YN = 'N'
    </select>

    <!-- 공지사항 목록 조회 (중요 공지 우선, 최신순) -->
    <select id="selectNoticeList" resultType="Notice">
        SELECT 
            NOTICE_NO,
            USER_ID,
            NOTICE_SUBJECT,
            NOTICE_CONTENT,
            NOTICE_WRITER,
            NOTICE_CREATEAT,
            NOTICE_IMPORTANT,
            NOTICE_ISNEW,
            VIEW_COUNT,
            DELETE_YN
        FROM NOTICE_TBL
        WHERE DELETE_YN = 'N'
        ORDER BY 
            NOTICE_IMPORTANT DESC,
            NOTICE_NO DESC
    </select>

    <!-- 검색 결과 전체 개수 조회 -->
    <select id="getSearchTotalCount" resultType="_int">
        SELECT COUNT(*) 
        FROM NOTICE_TBL
        WHERE DELETE_YN = 'N'
        <if test="searchCondition == 'title'">
            AND NOTICE_SUBJECT LIKE '%' || #{searchKeyword} || '%'
        </if>
        <if test="searchCondition == 'content'">
            AND NOTICE_CONTENT LIKE '%' || #{searchKeyword} || '%'
        </if>
        <if test="searchCondition == 'writer'">
            AND NOTICE_WRITER LIKE '%' || #{searchKeyword} || '%'
        </if>
    </select>

    <!-- 공지사항 검색 목록 조회 -->
    <select id="selectSearchList" resultType="Notice">
        SELECT 
            NOTICE_NO,
            USER_ID,
            NOTICE_SUBJECT,
            NOTICE_CONTENT,
            NOTICE_WRITER,
            NOTICE_CREATEAT,
            NOTICE_IMPORTANT,
            NOTICE_ISNEW,
            VIEW_COUNT,
            DELETE_YN
        FROM NOTICE_TBL
        WHERE DELETE_YN = 'N'
        <if test="searchCondition == 'title'">
            AND NOTICE_SUBJECT LIKE '%' || #{searchKeyword} || '%'
        </if>
        <if test="searchCondition == 'content'">
            AND NOTICE_CONTENT LIKE '%' || #{searchKeyword} || '%'
        </if>
        <if test="searchCondition == 'writer'">
            AND NOTICE_WRITER LIKE '%' || #{searchKeyword} || '%'
        </if>
        ORDER BY 
            NOTICE_IMPORTANT DESC,
            NOTICE_NO DESC
    </select>

    <!-- 공지사항 상세 조회 -->
    <select id="selectOneByNo" resultType="Notice">
        SELECT 
            NOTICE_NO,
            USER_ID,
            NOTICE_SUBJECT,
            NOTICE_CONTENT,
            NOTICE_WRITER,
            NOTICE_CREATEAT,
            NOTICE_IMPORTANT,
            NOTICE_ISNEW,
            VIEW_COUNT,
            DELETE_YN
        FROM NOTICE_TBL
        WHERE NOTICE_NO = #{noticeNo}
        AND DELETE_YN = 'N'
    </select>

    <!-- 조회수 증가 -->
    <update id="updateViewCount">
        UPDATE NOTICE_TBL
        SET VIEW_COUNT = VIEW_COUNT + 1
        WHERE NOTICE_NO = #{noticeNo}
        AND DELETE_YN = 'N'
    </update>

    <!-- 공지사항 등록 -->
    <insert id="insertNotice">
        INSERT INTO NOTICE_TBL (
            USER_ID,
            NOTICE_SUBJECT,
            NOTICE_CONTENT,
            NOTICE_WRITER,
            NOTICE_IMPORTANT,
            NOTICE_ISNEW
        ) VALUES (
            #{userId},
            #{noticeSubject},
            #{noticeContent},
            #{noticeWriter},
            #{noticeImportant, jdbcType=VARCHAR},
            #{noticeIsNew, jdbcType=VARCHAR}
        )
    </insert>

    <!-- 공지사항 수정 -->
    <update id="updateNotice">
        UPDATE NOTICE_TBL
        SET 
            NOTICE_SUBJECT = #{noticeSubject},
            NOTICE_CONTENT = #{noticeContent},
            NOTICE_IMPORTANT = #{noticeImportant, jdbcType=VARCHAR},
            NOTICE_ISNEW = #{noticeIsNew, jdbcType=VARCHAR}
        WHERE NOTICE_NO = #{noticeNo}
        AND DELETE_YN = 'N'
    </update>

    <!-- 공지사항 삭제 (논리 삭제) -->
    <update id="deleteNotice">
        UPDATE NOTICE_TBL
        SET DELETE_YN = 'Y'
        WHERE NOTICE_NO = #{noticeNo}
    </update>

</mapper>